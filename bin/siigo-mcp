#!/usr/bin/env node

void import('../dist/cli.js')
  .then(({ main }) => main())
  .catch((error) => {
    console.error('Failed to start Siigo MCP server:', error instanceof Error ? error.message : String(error));
    process.exitCode = 1;
  });
